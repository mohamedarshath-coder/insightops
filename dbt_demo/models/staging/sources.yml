version: 2
sources:
  - name: insightops
    database: "{{ 'BET_DB' if target.type == 'snowflake' else none }}"
    schema: INSIGHTOPS_RAW
    tables:
      - name: raw_orders
      - name: raw_customers
      - name: raw_products
